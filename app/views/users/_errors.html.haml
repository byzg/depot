- if @user.errors.any?
  #error_explanation
    %h2= "#{pluralize(@user.errors.count, "error")}, сведения об этом пользователе не могут быть сохранены:"
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg

